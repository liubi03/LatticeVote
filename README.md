# LatticeVote: A Post-Quantum Voting Demo

基于格密码的同态加密电子投票演示系统

## 项目介绍

本项目是**量子密码与后量子密码课程中期展示**作品，基于论文《A Homomorphic LWE Based E-voting Scheme》实现了一个基于 LWE（Learning With Errors）的同态加密投票系统。

### 项目背景

随着量子计算的发展，传统的基于数论困难问题（如 RSA、ECC）的加密方案面临被量子算法破解的风险。基于格密码的后量子密码学被认为是抵御量子计算攻击的最有前途的方向之一。本项目演示了如何利用 BFV 同态加密方案构建一个隐私保护的电子投票系统。

### 核心技术

- **BFV 同态加密方案**：基于 LWE 问题，支持在密文上直接进行加法运算
- **同态计票**：选票加密后直接累加，无需解密即可统计票数
- **隐私保护**：整个计票过程不暴露任何单张选票的内容
- **RSA 数字签名**：选民身份认证，防止伪造投票
- **零知识证明**：选票有效性证明，不泄露投票内容

## 核心原理简介

### 同态加密概念

同态加密是一种特殊的加密形式，允许对密文进行特定的代数运算，解密后的结果与对明文进行相同运算的结果一致。

```
Enc(m1) ⊕ Enc(m2) = Enc(m1 ⊕ m2)
```

本项目使用的是**加法同态**：可以在密文上执行加法操作。

### BFV 方案简介

BFV（Brakerski-Fan-Vercauteren）是一种基于格密码的全同态加密方案，本项目使用其加法同态特性：

| 参数 | 说明 | 本项目设置 |
|------|------|-----------|
| poly_modulus_degree | 多项式模次数 | 8192 |
| plain_modulus | 明文模数 | 1032193 |

### 投票流程说明

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   初始化    │ ──▶ │    注册     │ ──▶ │    投票     │ ──▶ │    计票     │
└─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘
      │                   │                   │                   │
      ▼                   ▼                   ▼                   ▼
  生成密钥对         选民身份登记        加密投票选择        同态累加选票
  创建公告板         防止重复注册        提交加密选票        解密公布结果
```

#### 1. 初始化阶段
- 生成 BFV 加密上下文（公钥、私钥）
- 创建选民注册表和公告板
- 设置候选人信息

#### 2. 注册阶段
- 选民提交身份信息
- 系统验证身份并登记
- 防止重复注册

#### 3. 投票阶段
- 选民选择候选人
- 系统将选择编码为 one-hot 向量
- 使用公钥加密选票
- 提交加密选票到公告板

#### 4. 计票阶段
- 对所有加密选票进行同态加法
- 一次性解密累加结果
- 公布各候选人得票数

### 安全性优势

| 特性 | 说明 |
|------|------|
| **隐私保护** | 选票以密文形式存储，任何人无法查看单张选票内容 |
| **防篡改** | 同态加密保证计票过程不可被操纵 |
| **防重复投票** | 选民注册表确保一人一票 |
| **可验证性** | 计票结果可通过总票数验证 |
| **身份认证** | RSA 数字签名确保投票者身份 |
| **选票有效性** | 零知识证明验证选票格式正确 |

## 环境配置说明

### 系统要求

- **Python 版本**：3.10 或以上
- **操作系统**：Windows / Linux / macOS
- **推荐环境管理工具**：Conda

### 环境配置（推荐）

本项目已配置好 Conda 虚拟环境 `LatticeVote`，包含所有必要依赖。

#### 激活环境

```bash
conda activate LatticeVote
```

#### 验证环境

```bash
python --version  # 应显示 Python 3.10.x
pip list          # 查看已安装的包
```

### 手动安装依赖（可选）

如果需要在新环境中安装依赖：

```bash
pip install -r code/requirements.txt
```

### 核心依赖

| 依赖包 | 版本要求 | 用途 |
|--------|---------|------|
| [tenseal](https://github.com/OpenMined/TenSEAL) | 最新版 | BFV 同态加密实现 |
| [streamlit](https://streamlit.io/) | 最新版 | Web 交互界面 |
| numpy | 最新版 | 数值计算 |
| plotly | 最新版 | 数据可视化 |
| pandas | 最新版 | 数据处理 |
| cryptography | 最新版 | RSA 数字签名 |

## 运行说明

### 方式一：命令行演示

运行完整的投票流程演示：

```bash
python code/demo/run_vote.py
```

**预期输出示例**：

```
============================================================
  LatticeVote 电子投票系统演示
============================================================

本演示展示基于格密码的同态加密电子投票系统
支持隐私保护的投票和同态计票功能

============================================================
  阶段 1: 系统初始化
============================================================

----------------------------------------
  初始化 BFV 加密上下文
----------------------------------------
正在创建 BFV 同态加密上下文...
BFV 上下文初始化成功:
  - 多项式模次数: 8192
  - 明文模数: 1032193
加密上下文初始化完成

...

============================================================
  阶段 5: 结果展示
============================================================

----------------------------------------
  投票结果统计
----------------------------------------

投票结果统计:
  候选人 1 (Alice): 2 票
  候选人 2 (Bob): 2 票
  候选人 3 (Charlie): 1 票
  候选人 4 (Diana): 2 票
```

### 方式二：Web 交互界面（演示版本）

启动 Streamlit Web 应用：

```bash
cd code/web
streamlit run app.py
```

浏览器访问 `http://localhost:8501` 即可使用交互式投票系统。

### 方式三：账户系统 Web 界面（完整版本）⭐ 推荐

启动完整的账户系统 Web 应用：

```bash
# 激活 Conda 环境
conda activate LatticeVote

# 运行账户系统
cd code/account_system
streamlit run web_app.py
```

浏览器访问 `http://localhost:8501` 即可使用完整的投票系统。

## 账户系统详细说明

### 默认账户

#### 管理员账户
| 字段 | 值 |
|------|-----|
| 用户名 | `admin` |
| 密码 | `admin123` |

#### 预设选民账户
| 用户名 | 密码 | 角色 |
|--------|------|------|
| `liubi01` | `123456` | 选民 |
| `liubi02` | `123456` | 选民 |
| `liubi03` | `123456` | 选民 |
| `liubi04` | `123456` | 选民 |
| `liubi05` | `123456` | 选民 |

#### 预设受托人账户
| 用户名 | 密码 | 角色 |
|--------|------|------|
| `liubi11` | `123456` | 受托人 |
| `liubi12` | `123456` | 受托人 |

### 初始化预设账户

```bash
conda activate LatticeVote
python code/account_system/init_accounts.py
```

### 完整投票流程

#### 步骤 1：管理员创建投票项目
1. 登录管理员账户（admin / admin123）
2. 进入「投票项目」标签
3. 填写项目名称、描述、候选人列表
4. 点击「创建项目」

#### 步骤 2：指定选民和受托人
1. 在项目管理区域展开项目
2. 选择受托人（liubi11, liubi12）
3. 选择选民（liubi01-05）
4. 点击「更新受托人」和「更新选民」

#### 步骤 3：启动投票
1. 点击「▶️ 启动」按钮
2. 项目状态变为「进行中」

#### 步骤 4：选民投票
1. 登录选民账户（如 liubi01 / 123456）
2. 选择候选人
3. 输入私钥（从 `code/account_system/data/users.json` 复制对应账户的 `private_key` 字段）
4. 点击「🗳️ 提交投票」

#### 步骤 5：管理员结束投票
1. 登录管理员账户
2. 点击「⏹️ 结束」按钮
3. 项目状态变为「已结束」

#### 步骤 6：受托人计票
1. 登录受托人账户（liubi11 / 123456）
2. 点击「📊 执行计票」
3. 查看投票结果

#### 步骤 7：投票过程演示（中期展示）
1. 登录管理员账户
2. 进入「投票演示」标签
3. 选择演示模式：
   - **📋 模拟演示**：使用模拟数据展示流程
   - **📊 真实投票演示**：展示真实投票数据和计票结果
4. 选择项目后点击「开始演示」

### 功能说明

#### 管理员功能
- 审核用户注册（批准/拒绝）
- 创建和管理投票项目
- 指定选民和受托人
- 启动/暂停/结束投票项目
- **投票过程演示**（详细展示加密、解密、同态运算过程）

#### 受托人功能
- 查看参与的投票项目
- 执行计票操作
- 查看计票结果

#### 选民功能
- 查看可投票的项目
- 提交加密投票
- 使用 RSA 私钥签名认证

## 项目结构

```
LatticeVote/
├── README.md                   # 项目说明文档
├── code/                       # 源代码目录
│   ├── __init__.py
│   ├── requirements.txt        # Python 依赖
│   ├── backend/                # 后端核心模块
│   │   ├── __init__.py
│   │   ├── crypto.py          # BFV 同态加密实现
│   │   ├── voter.py           # 选民管理与投票逻辑
│   │   ├── tally.py           # 同态计票实现
│   │   └── utils.py           # 工具函数
│   ├── demo/                   # 命令行演示
│   │   └── run_vote.py        # 完整投票流程演示
│   ├── web/                    # Web 界面（演示版本）
│   │   └── app.py             # Streamlit 应用
│   └── account_system/         # 账户系统（完整版本）
│       ├── __init__.py
│       ├── users.py           # 用户管理
│       ├── auth.py            # 认证模块
│       ├── signature.py       # 数字签名
│       ├── zkp.py             # 零知识证明
│       ├── voting_project.py  # 投票项目管理
│       ├── ballot.py          # 选票管理
│       ├── init_accounts.py   # 预设账户初始化
│       ├── web_app.py         # Web 界面
│       └── data/              # 数据存储
│           ├── users.json     # 用户数据（含私钥）
│           └── projects.json  # 项目数据
├── 论文/                       # 参考论文
│   └── A Homomorphic LWE Based E-voting Scheme.pdf
└── .trae/                      # 项目配置
    └── specs/
```

### 模块说明

| 模块 | 文件 | 功能描述 |
|------|------|---------|
| **加密模块** | `crypto.py` | BFV 上下文管理、密钥生成、加密/解密、同态加法 |
| **选民模块** | `voter.py` | 选民注册、身份验证、选票创建、公告板管理 |
| **计票模块** | `tally.py` | 同态累加、结果解密、计票验证 |
| **工具模块** | `utils.py` | One-hot 编码、候选人名称生成、结果格式化 |
| **演示模块** | `run_vote.py` | 完整投票流程的命令行演示 |
| **Web模块** | `app.py` | Streamlit 交互式 Web 界面（演示版本）|
| **用户管理** | `users.py` | 用户注册、登录、审核、权限管理 |
| **认证模块** | `auth.py` | 会话管理、权限检查、登录装饰器 |
| **数字签名** | `signature.py` | RSA 密钥对生成、签名生成与验证 |
| **零知识证明** | `zkp.py` | 选民身份证明、选票有效性证明、计票正确性证明 |
| **项目管理** | `voting_project.py` | 投票项目创建、状态管理、选民/受托人指定 |
| **选票管理** | `ballot.py` | 选票创建、加密、签名、验证、计票 |
| **账户系统Web** | `web_app.py` | 完整的账户系统 Web 界面（含投票过程演示）|

## 技术特点

### 1. 同态加密保护投票隐私

```python
# 选票加密示例
choice_vector = [0, 1, 0, 0]  # 投票给第2位候选人
encrypted_vote = bfv.encrypt_vector(choice_vector)

# 同态计票：密文直接相加
encrypted_sum = encrypted_vote1 + encrypted_vote2 + ...

# 最终解密：只解密累加结果
vote_counts = bfv.decrypt(encrypted_sum)  # [2, 5, 1, 3]
```

### 2. RSA 数字签名认证

```python
# 选民注册时生成密钥对
private_key, public_key = DigitalSignature.generate_key_pair()

# 投票时签名
signature = ds.sign(ballot_data)

# 验证签名
is_valid = ds.verify(ballot_data, signature)
```

### 3. 零知识证明

```python
# 生成选票有效性证明
proof = SimplifiedZKP.generate_ballot_validity_proof(
    choice_index, num_candidates, encrypted_ballot_hash
)

# 验证证明（不泄露投票内容）
is_valid = SimplifiedZKP.verify_ballot_validity_proof(proof, ballot_hash)
```

### 4. 防止重复投票

- 选民注册表记录所有已注册选民
- 公告板追踪已投票选民 ID
- 提交选票时自动验证投票资格

### 5. 可验证的计票结果

- 总票数与选民数一致性验证
- 各候选人得票数非负性检查
- 计票结果完整性验证

### 6. 交互式 Web 界面

- 直观的投票流程引导
- 实时投票进度显示
- 可视化计票结果（柱状图）
- 响应式界面设计
- **投票过程演示功能**（适合中期展示）

## 简化说明

### 与论文协议的差异

本演示版本对论文《A Homomorphic LWE Based E-voting Scheme》中的协议进行了适当简化：

| 特性 | 论文协议 | 本演示版本 |
|------|---------|-----------|
| 零知识证明 | 完整的投票有效性证明 | 简化版实现 |
| 混合网络 | 多服务器分布式架构 | 单机演示 |
| 选民认证 | 数字签名认证 | ✅ RSA 数字签名 |
| 密钥管理 | 分布式密钥生成 | 单方密钥生成 |
| 审计追踪 | 完整审计日志 | 基础日志记录 |

### 后续完善方向

1. **安全性增强**
   - 实现完整的零知识证明协议
   - 实现分布式密钥生成协议
   - 增加选民身份的强认证机制

2. **功能扩展**
   - 支持权重投票
   - 实现排名选择投票
   - 添加弃权票处理

3. **性能优化**
   - 批量加密处理
   - 并行同态计算
   - 大规模选民测试

4. **工程完善**
   - 数据库持久化存储
   - RESTful API 接口
   - 完整的单元测试覆盖

## 参考资料

- [TenSEAL 官方文档](https://github.com/OpenMined/TenSEAL)
- [Microsoft SEAL](https://github.com/microsoft/SEAL)
- [BFV 方案论文](https://eprint.iacr.org/2012/144)
- 论文：A Homomorphic LWE Based E-voting Scheme

## 许可证

本项目仅用于学术演示和教育目的。

---

**LatticeVote** - 基于格密码的同态加密电子投票系统 | 量子密码与后量子密码课程中期展示
